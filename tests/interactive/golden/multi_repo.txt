$ rein run --direct -- bash -c <read+declare TomHennen/agentcreds-validation-a> /tmp/rein-itest-y6k6cr3o
===============================================================
rein: WARNING — DIRECT (UNSANDBOXED) MODE
  The agent runs OUTSIDE the srt sandbox. It shares your user
  account: it CAN read your ambient credentials (gh login, SSH
  keys, ~/.netrc, keyrings) and the rein-brokered token is only
  hidden by process boundaries, not a sandbox. Use this ONLY on a
  THROWAWAY repo. For real work, drop --direct to run sandboxed.
===============================================================
rein: launching wrapped process with:
  session: sess_journey_multirepo (role=implement, repos=[TomHennen/agentcreds-validation-a TomHennen/agentcreds-validation-b]) [source=file:/tmp/rein-journey-sess-t6qiq2rd/session.yaml]
  PATH-front shim dir: /home/admin/.local/state/rein/shim
  per-process git config: /tmp/rein-run-3035287473/gitconfig
  (your real ~/.gitconfig is layered in via include.path)

  Writes are LOCKED until the agent declares its issue:  rein declare <n>
  then push to agent/<n>/<nonce>. The declaration will prompt on THIS terminal
  (or a tmux popup); approving covers all writes for this run.

rein: running: bash -c
set +e
emit() { printf '%s%s\n' 'SBX| ' "$*"; }
run() {
  printf '%s$ %s\n' 'SBX| ' "$*"
  "$@" 2>&1 | tr '\r' '\n' | while IFS= read -r l; do printf '%s%s\n' 'SBX| ' "$l"; done
  return ${PIPESTATUS[0]}
}
cd "$0"

emit "@READ_START  clone TomHennen/agentcreds-validation-a (read flows with no declaration)"
rm -rf repo
run git clone https://github.com/TomHennen/agentcreds-validation-a repo
emit "@READ_RC=$?"
[ -d repo/.git ] && emit "@READ_OK"

emit "@DECLARE_START  rein declare 85 --repo TomHennen/agentcreds-validation-a  (in scope: plain confirm, NOT expansion)"
run rein declare 85 --repo TomHennen/agentcreds-validation-a
emit "@DECLARE_RC=$?"
emit "@SCRIPT_DONE"
 /tmp/rein-itest-y6k6cr3o
---
SBX| @READ_START  clone TomHennen/agentcreds-validation-a (read flows with no declaration)
SBX| $ git clone https://github.com/TomHennen/agentcreds-validation-a repo
SBX| Cloning into 'repo'...
SBX| @READ_RC=0
SBX| @READ_OK
SBX| @DECLARE_START  rein declare 85 --repo TomHennen/agentcreds-validation-a  (in scope: plain confirm, NOT expansion)
SBX| $ rein declare 85 --repo TomHennen/agentcreds-validation-a

=== rein: agent declares work on an issue ===
   issue:    #85 "rein journey: multi-repo A fixture (safe to close)"  [open]
             in TomHennen/agentcreds-validation-a
   session:  sess_journey_multirepo (role=implement, repos=[TomHennen/agentcreds-validation-a, TomHennen/agentcreds-validation-b])
   approving covers ALL writes for this run (git push, gh, API).

To approve, type the issue number (85) and press enter.
To deny, press Ctrl-C or type anything else.
> 85
  [approved]
SBX| issue #85 in TomHennen/agentcreds-validation-a confirmed — writes are unlocked for this run (push to agent/85/<nonce>)
SBX| @DECLARE_RC=0
SBX| @SCRIPT_DONE

$ rein run --direct -- bash -c <read+declare TomHennen/agentcreds-validation-b> /tmp/rein-itest-93bfz3hm
===============================================================
rein: WARNING — DIRECT (UNSANDBOXED) MODE
  The agent runs OUTSIDE the srt sandbox. It shares your user
  account: it CAN read your ambient credentials (gh login, SSH
  keys, ~/.netrc, keyrings) and the rein-brokered token is only
  hidden by process boundaries, not a sandbox. Use this ONLY on a
  THROWAWAY repo. For real work, drop --direct to run sandboxed.
===============================================================
rein: launching wrapped process with:
  session: sess_journey_multirepo (role=implement, repos=[TomHennen/agentcreds-validation-a TomHennen/agentcreds-validation-b]) [source=file:/tmp/rein-journey-sess-t6qiq2rd/session.yaml]
  PATH-front shim dir: /home/admin/.local/state/rein/shim
  per-process git config: /tmp/rein-run-2916835333/gitconfig
  (your real ~/.gitconfig is layered in via include.path)

  Writes are LOCKED until the agent declares its issue:  rein declare <n>
  then push to agent/<n>/<nonce>. The declaration will prompt on THIS terminal
  (or a tmux popup); approving covers all writes for this run.

rein: running: bash -c
set +e
emit() { printf '%s%s\n' 'SBX| ' "$*"; }
run() {
  printf '%s$ %s\n' 'SBX| ' "$*"
  "$@" 2>&1 | tr '\r' '\n' | while IFS= read -r l; do printf '%s%s\n' 'SBX| ' "$l"; done
  return ${PIPESTATUS[0]}
}
cd "$0"

emit "@READ_START  clone TomHennen/agentcreds-validation-b (read flows with no declaration)"
rm -rf repo
run git clone https://github.com/TomHennen/agentcreds-validation-b repo
emit "@READ_RC=$?"
[ -d repo/.git ] && emit "@READ_OK"

emit "@DECLARE_START  rein declare 5 --repo TomHennen/agentcreds-validation-b  (in scope: plain confirm, NOT expansion)"
run rein declare 5 --repo TomHennen/agentcreds-validation-b
emit "@DECLARE_RC=$?"
emit "@SCRIPT_DONE"
 /tmp/rein-itest-93bfz3hm
---
SBX| @READ_START  clone TomHennen/agentcreds-validation-b (read flows with no declaration)
SBX| $ git clone https://github.com/TomHennen/agentcreds-validation-b repo
SBX| Cloning into 'repo'...
SBX| @READ_RC=0
SBX| @READ_OK
SBX| @DECLARE_START  rein declare 5 --repo TomHennen/agentcreds-validation-b  (in scope: plain confirm, NOT expansion)
SBX| $ rein declare 5 --repo TomHennen/agentcreds-validation-b

=== rein: agent declares work on an issue ===
   issue:    #5 "rein journey: multi-repo B fixture (safe to close)"  [open]
             in TomHennen/agentcreds-validation-b
   session:  sess_journey_multirepo (role=implement, repos=[TomHennen/agentcreds-validation-a, TomHennen/agentcreds-validation-b])
   approving covers ALL writes for this run (git push, gh, API).

To approve, type the issue number (5) and press enter.
To deny, press Ctrl-C or type anything else.
> 5
  [approved]
SBX| issue #5 in TomHennen/agentcreds-validation-b confirmed — writes are unlocked for this run (push to agent/5/<nonce>)
SBX| @DECLARE_RC=0
SBX| @SCRIPT_DONE
