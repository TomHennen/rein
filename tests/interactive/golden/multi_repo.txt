$ rein run --direct -- bash -c <clone+declare+push A and B> /tmp/rein-itest-xsg6_yyp
===============================================================
rein: WARNING — DIRECT (UNSANDBOXED) MODE
  The agent runs OUTSIDE the srt sandbox. It shares your user
  account: it CAN read your ambient credentials (gh login, SSH
  keys, ~/.netrc, keyrings) and the rein-brokered token is only
  hidden by process boundaries, not a sandbox. Use this ONLY on a
  THROWAWAY repo. For real work, drop --direct to run sandboxed.
===============================================================
rein: launching wrapped process with:
  session: sess_journey_multirepo (role=implement, repos=[TomHennen/agentcreds-validation-a TomHennen/agentcreds-validation-b]) [source=file:/tmp/rein-journey-sess-eggr8roa/session.yaml]
  PATH-front shim dir: /home/admin/.local/state/rein/shim
  per-process git config: /tmp/rein-run-391348203/gitconfig
  (your real ~/.gitconfig is layered in via include.path)

  Writes are LOCKED until the agent declares its issue:  rein declare <n>
  then push to agent/<n>/<nonce>. The declaration will prompt on THIS terminal
  (or a tmux popup); approving covers all writes for this run.

rein: running: bash -c
set +e
emit() { printf '%s%s\n' 'SBX| ' "$*"; }
run() {
  printf '%s$ %s\n' 'SBX| ' "$*"
  "$@" 2>&1 | tr '\r' '\n' | while IFS= read -r l; do printf '%s%s\n' 'SBX| ' "$l"; done
  return ${PIPESTATUS[0]}
}
cd "$0"

emit "@READ_A_START  clone TomHennen/agentcreds-validation-a (read flows with no declaration)"
rm -rf repoA
run git clone https://github.com/TomHennen/agentcreds-validation-a repoA
emit "@READ_A_RC=$?"
[ -d repoA/.git ] && emit "@READ_A_OK"

emit "@READ_B_START  clone TomHennen/agentcreds-validation-b (the SECOND session repo — reads flow too)"
rm -rf repoB
run git clone https://github.com/TomHennen/agentcreds-validation-b repoB
emit "@READ_B_RC=$?"
[ -d repoB/.git ] && emit "@READ_B_OK"

emit "@DECLARE_A_START  rein declare 116 --repo TomHennen/agentcreds-validation-a  (1st issue: plain confirm, in scope)"
run rein declare 116 --repo TomHennen/agentcreds-validation-a
emit "@DECLARE_A_RC=$?"

emit "@PUSH_A_START  push agent/116/<nonce> -> TomHennen/agentcreds-validation-a (expect: LANDS on A)"
cd repoA
echo "multi-repo journey: real agent work in repo A" >> agent-note.txt
run git add -A
run git commit -q -m "multi-repo journey: real agent work (repo A)"
run git push --progress origin HEAD:refs/heads/agent/116/mr-a-20260712-183554-f21620
emit "@PUSH_A_RC=$?"
cd "$0"

emit "@DECLARE_B_START  rein declare 15 --repo TomHennen/agentcreds-validation-b  (2nd issue mid-run: the 'ALSO work on an issue' confirm; B is already in scope, so NOT a repo scope-expansion)"
run rein declare 15 --repo TomHennen/agentcreds-validation-b
emit "@DECLARE_B_RC=$?"

emit "@PUSH_B_START  push agent/15/<nonce> -> TomHennen/agentcreds-validation-b (expect: LANDS on B)"
cd repoB
echo "multi-repo journey: real agent work in repo B" >> agent-note.txt
run git add -A
run git commit -q -m "multi-repo journey: real agent work (repo B)"
run git push --progress origin HEAD:refs/heads/agent/15/mr-b-20260712-183554-03227c
emit "@PUSH_B_RC=$?"
emit "@SCRIPT_DONE"
 /tmp/rein-itest-xsg6_yyp
---
SBX| @READ_A_START  clone TomHennen/agentcreds-validation-a (read flows with no declaration)
SBX| $ git clone https://github.com/TomHennen/agentcreds-validation-a repoA
SBX| Cloning into 'repoA'...
SBX| @READ_A_RC=0
SBX| @READ_A_OK
SBX| @READ_B_START  clone TomHennen/agentcreds-validation-b (the SECOND session repo — reads flow too)
SBX| $ git clone https://github.com/TomHennen/agentcreds-validation-b repoB
SBX| Cloning into 'repoB'...
SBX| @READ_B_RC=0
SBX| @READ_B_OK
SBX| @DECLARE_A_START  rein declare 116 --repo TomHennen/agentcreds-validation-a  (1st issue: plain confirm, in scope)
SBX| $ rein declare 116 --repo TomHennen/agentcreds-validation-a

=== rein: agent declares work on an issue ===
   issue:    #116 "rein journey: multi-repo fixture (safe to close)"  [open]
             in TomHennen/agentcreds-validation-a
   session:  sess_journey_multirepo (role=implement, repos=[TomHennen/agentcreds-validation-a, TomHennen/agentcreds-validation-b])
   approving covers ALL writes for this run (git push, gh, API).

To approve, type the issue number (116) and press enter.
To deny, press Ctrl-C or type anything else.
> 116
  [approved]
SBX| issue #116 in TomHennen/agentcreds-validation-a confirmed — writes are unlocked for this run (push to agent/116/<nonce>)
SBX| @DECLARE_A_RC=0
SBX| @PUSH_A_START  push agent/116/<nonce> -> TomHennen/agentcreds-validation-a (expect: LANDS on A)
SBX| $ git add -A
SBX| $ git commit -q -m multi-repo journey: real agent work (repo A)
SBX| $ git push --progress origin HEAD:refs/heads/agent/116/mr-a-20260712-183554-f21620
SBX| Enumerating objects: 4, done.
SBX| Counting objects: 100% (4/4), done.
SBX| Delta compression using up to 8 threads
SBX| Compressing objects: 100% (2/2), done.
SBX| Writing objects: 100% (3/3), 332 bytes | 332.00 KiB/s, done.
SBX| Total 3 (delta 1), reused 0 (delta 0), pack-reused 0
SBX| remote:
SBX| remote: Create a pull request for 'agent/116/mr-a-20260712-183554-f21620' on GitHub by visiting:
SBX| remote:      https://github.com/TomHennen/agentcreds-validation-a/pull/new/agent/116/mr-a-20260712-183554-f21620
SBX| remote:
SBX| To https://github.com/TomHennen/agentcreds-validation-a
SBX|  * [new branch]      HEAD -> agent/116/mr-a-20260712-183554-f21620
SBX| @PUSH_A_RC=0
SBX| @DECLARE_B_START  rein declare 15 --repo TomHennen/agentcreds-validation-b  (2nd issue mid-run: the 'ALSO work on an issue' confirm; B is already in scope, so NOT a repo scope-expansion)
SBX| $ rein declare 15 --repo TomHennen/agentcreds-validation-b

=== rein: agent wants to ALSO work on an issue ===
   issue:    #15 "rein journey: multi-repo fixture (safe to close)"  [open]
             in TomHennen/agentcreds-validation-b
   session:  sess_journey_multirepo (role=implement, repos=[TomHennen/agentcreds-validation-a, TomHennen/agentcreds-validation-b])
   approving covers ALL writes for this run (git push, gh, API).

To approve, type the issue number (15) and press enter.
To deny, press Ctrl-C or type anything else.
> 15
  [approved]
SBX| issue #15 in TomHennen/agentcreds-validation-b confirmed — writes are unlocked for this run (push to agent/15/<nonce>)
SBX| @DECLARE_B_RC=0
SBX| @PUSH_B_START  push agent/15/<nonce> -> TomHennen/agentcreds-validation-b (expect: LANDS on B)
SBX| $ git add -A
SBX| $ git commit -q -m multi-repo journey: real agent work (repo B)
SBX| $ git push --progress origin HEAD:refs/heads/agent/15/mr-b-20260712-183554-03227c
SBX| Enumerating objects: 4, done.
SBX| Counting objects: 100% (4/4), done.
SBX| Delta compression using up to 8 threads
SBX| Compressing objects: 100% (2/2), done.
SBX| Writing objects: 100% (3/3), 361 bytes | 361.00 KiB/s, done.
SBX| Total 3 (delta 0), reused 0 (delta 0), pack-reused 0
SBX| remote:
SBX| remote: Create a pull request for 'agent/15/mr-b-20260712-183554-03227c' on GitHub by visiting:
SBX| remote:      https://github.com/TomHennen/agentcreds-validation-b/pull/new/agent/15/mr-b-20260712-183554-03227c
SBX| remote:
SBX| To https://github.com/TomHennen/agentcreds-validation-b
SBX|  * [new branch]      HEAD -> agent/15/mr-b-20260712-183554-03227c
SBX| @PUSH_B_RC=0
SBX| @SCRIPT_DONE
rein: revoked 2 of 2 write token(s) on exit
