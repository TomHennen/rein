$ rein run --direct -- bash -c <direct-ceremony> /tmp/rein-itest-viy47msj
===============================================================
rein: WARNING — DIRECT (UNSANDBOXED) MODE
  The agent runs OUTSIDE the nono sandbox. It shares your user
  account: it CAN read your ambient credentials (gh login, SSH
  keys, ~/.netrc, keyrings) and the rein-brokered token is only
  hidden by process boundaries, not a sandbox. Use this ONLY on a
  THROWAWAY repo. For real work, drop --direct to run sandboxed.
===============================================================
rein: launching wrapped process with:
  session: sess_journey_directmode (role=implement, repos=[TomHennen/agentcreds-validation-a]) [source=file:/tmp/rein-journey-sess-kh9w3m7y/session.yaml]
  PATH-front shim dir: /home/admin/.local/state/rein/shim
  per-process git config: /tmp/rein-run-609673684/gitconfig
  (your real ~/.gitconfig is layered in via include.path)

  Writes are LOCKED until the agent declares its issue:  rein declare <n>
  then push to agent/<n>/<nonce>. The declaration will prompt on THIS terminal
  (or a tmux popup); approving covers all writes for this run.

rein: running: bash -c
set +e
emit() { printf '%s%s\n' 'SBX| ' "$*"; }
run() {
  printf '%s$ %s\n' 'SBX| ' "$*"
  "$@" 2>&1 | tr '\r' '\n' | while IFS= read -r l; do printf '%s%s\n' 'SBX| ' "$l"; done
  return ${PIPESTATUS[0]}
}
cd "$0"
rm -rf repo
run git clone https://github.com/TomHennen/agentcreds-validation-a repo
cd repo || { emit "@CLONE_FAIL"; exit 3; }
emit "@CLONE_OK  (reads flow with no declaration — direct mode, no sandbox)"

emit "@PHASE1_START  push BEFORE declare (expect: helper placeholder -> git auth fails)"
echo "direct phase 1" >> probe-1.txt
run git add -A
run git commit -q -m "direct ceremony: pre-declaration write attempt"
run git push --progress origin HEAD:refs/heads/agent/349/direct-20260719-021946-c3a51a
emit "@PHASE1_RC=$?"

emit "@PHASE2_START  rein declare 349 (blocks for the human on THIS terminal)"
run rein declare 349
emit "@PHASE2_RC=$?"

emit "@PHASE3_START  push agent/349/<nonce> (expect: lands)"
run git push --progress origin HEAD:refs/heads/agent/349/direct-20260719-021946-c3a51a
emit "@PHASE3_RC=$?"
emit "@SCRIPT_DONE"
 /tmp/rein-itest-viy47msj
---
SBX| $ git clone https://github.com/TomHennen/agentcreds-validation-a repo
SBX| Cloning into 'repo'...
SBX| @CLONE_OK  (reads flow with no declaration — direct mode, no sandbox)
SBX| @PHASE1_START  push BEFORE declare (expect: helper placeholder -> git auth fails)
SBX| $ git add -A
SBX| $ git commit -q -m direct ceremony: pre-declaration write attempt
SBX| $ git push --progress origin HEAD:refs/heads/agent/349/direct-20260719-021946-c3a51a
SBX|
SBX| rein: no issue declared for this run — writes are locked.
SBX|   Run: rein declare <n>   (the issue number this work is for)
SBX|   approve on your terminal, then retry this operation.
SBX|
SBX| remote: Invalid username or token. Password authentication is not supported for Git operations.
SBX| fatal: Authentication failed for 'https://github.com/TomHennen/agentcreds-validation-a/'
SBX| @PHASE1_RC=128
SBX| @PHASE2_START  rein declare 349 (blocks for the human on THIS terminal)
SBX| $ rein declare 349

=== rein: agent declares work on an issue ===
   issue:    #349 "rein journey: direct-mode walkthrough (safe to close)"  [open]
             in TomHennen/agentcreds-validation-a
   session:  sess_journey_directmode (role=implement, repos=[TomHennen/agentcreds-validation-a])
   approving covers ALL writes for this run (git push, gh, API).

To approve, type the issue number (349) and press enter.
To deny, press Ctrl-C or type anything else.
> 349
  [approved]
SBX| issue #349 in TomHennen/agentcreds-validation-a confirmed — writes are unlocked for this run (push to agent/349/<nonce>)
SBX| @PHASE2_RC=0
SBX| @PHASE3_START  push agent/349/<nonce> (expect: lands)
SBX| $ git push --progress origin HEAD:refs/heads/agent/349/direct-20260719-021946-c3a51a
SBX| Enumerating objects: 4, done.
SBX| Counting objects: 100% (4/4), done.
SBX| Delta compression using up to 8 threads
SBX| Compressing objects: 100% (2/2), done.
SBX| Writing objects: 100% (3/3), 311 bytes | 311.00 KiB/s, done.
SBX| Total 3 (delta 1), reused 0 (delta 0), pack-reused 0
SBX| remote:
SBX| remote: Create a pull request for 'agent/349/direct-20260719-021946-c3a51a' on GitHub by visiting:
SBX| remote:      https://github.com/TomHennen/agentcreds-validation-a/pull/new/agent/349/direct-20260719-021946-c3a51a
SBX| remote:
SBX| To https://github.com/TomHennen/agentcreds-validation-a
SBX|  * [new branch]      HEAD -> agent/349/direct-20260719-021946-c3a51a
SBX| @PHASE3_RC=0
SBX| @SCRIPT_DONE
rein: revoked 1 of 1 write token(s) on exit
