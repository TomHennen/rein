$ rein run -- bash -c <git-author-ceremony> /tmp/rein-itest-xyht51at
rein: worktrees: could not detect a GitHub repo for the working tree /tmp/rein-itest-xyht51at (git -C /tmp/rein-itest-xyht51at rev-parse --show-toplevel: exit status 128); the agent is not told which repo it is standing in
rein: verifying nono containment (creds hidden; loopback + direct egress denied)…
rein: containment WARNING: udp_egress: UDP egress OPEN (accepted residual — a prompt-injected agent has a UDP exfil channel) [8.8.8.8:53]
rein: containment gate passed.
rein: launching SANDBOXED (nono) run:
  session: sess_journey_gitauthor (role=implement, repos=[TomHennen/agentcreds-validation-a]) [source=file:/tmp/rein-journey-sess-av5saces/session.yaml]
  GitHub egress tunnels through rein's loopback proxy: 127.0.0.1:43437
  the agent sees NO real token; git/gh are injected at the proxy (downstream of the sandbox).
  credentials are hidden (deny_credentials); the approval channel is isolated (af_unix_mediation).
  writable in sandbox (--allow): [/tmp/rein-itest-xyht51at /home/admin/.config/rein-sandbox-home/.claude]
  extra egress ALLOWED (direct, NOT injected, no rein token): [api.anthropic.com]
  writes are LOCKED until the agent declares its issue:  rein declare <n>
  the declaration will prompt on THIS terminal.
rein: running: bash -c
set +e
emit() { printf '%s%s\n' 'SBX| ' "$*"; }
run() {
  printf '%s$ %s\n' 'SBX| ' "$*"
  "$@" 2>&1 | tr '\r' '\n' | while IFS= read -r l; do printf '%s%s\n' 'SBX| ' "$l"; done
  return ${PIPESTATUS[0]}
}
cd "$0"
rm -rf repo
run git clone https://github.com/TomHennen/agentcreds-validation-a repo
cd repo || { emit "@CLONE_FAIL"; exit 3; }
emit "@CLONE_OK  (read flows with no declaration)"

emit "@PHASE1_START  rein declare 351 (blocks for the human)"
run rein declare 351
emit "@PHASE1_RC=$?"

emit "@PHASE2_START  commit a probe (rein stamps GIT_AUTHOR_* AND GIT_COMMITTER_* = the delegated identity)"
echo "git-author probe $(date -u +%FT%TZ)" >> author-probe.txt
run git add -A
run git commit -q -m "git-author journey: delegated authorship"
emit "@PHASE2_RC=$?"
al=$(git log -1 --format='%an <%ae>')
emit "@AUTHOR $al"
cl=$(git log -1 --format='%cn <%ce>')
emit "@COMMITTER $cl"

emit "@PHASE3_START  push agent/351/<nonce> (expect: lands)"
run git push --progress origin HEAD:refs/heads/agent/351/author-20260719-022002-a561df
emit "@PHASE3_RC=$?"
emit "@SCRIPT_DONE"
 /tmp/rein-itest-xyht51at
---

  nono v0.68.0
  Capabilities:
  ────────────────────────────────────────────────────
    r   /tmp/rein-nono-38908843/ca-bundle.pem (file)
   r+w  /tmp/rein-itest-xyht51at (dir)
   r+w  /home/admin/.config/rein-sandbox-home/.claude (dir)
    r   /tmp/rein-nono-38908843/rein (file)
       + 49 system/group paths (-v to show)
   net  proxy
  ────────────────────────────────────────────────────

  mode supervised (proxy, supervisor)
  Applying sandbox...

SBX| $ git clone https://github.com/TomHennen/agentcreds-validation-a repo
SBX| Cloning into 'repo'...
SBX| @CLONE_OK  (read flows with no declaration)
SBX| @PHASE1_START  rein declare 351 (blocks for the human)
SBX| $ rein declare 351

=== rein: agent declares work on an issue ===
   issue:    #351 "rein journey: git-author walkthrough (safe to close)"  [open]
             in TomHennen/agentcreds-validation-a
   session:  sess_journey_gitauthor (role=implement, repos=[TomHennen/agentcreds-validation-a])
   approving covers ALL writes for this run (git push, gh, API).

To approve, type the issue number (351) and press enter.
To deny, press Ctrl-C or type anything else.
> 351
  [approved]
SBX| issue #351 in TomHennen/agentcreds-validation-a confirmed — writes are unlocked for this run (push to agent/351/<nonce>)
SBX| @PHASE1_RC=0
SBX| @PHASE2_START  commit a probe (rein stamps GIT_AUTHOR_* AND GIT_COMMITTER_* = the delegated identity)
SBX| $ git add -A
SBX| $ git commit -q -m git-author journey: delegated authorship
SBX| @PHASE2_RC=0
SBX| @AUTHOR Tom Hennen (via rein) <287259336+agentcreds-validation-beef[bot]@users.noreply.github.com>
SBX| @COMMITTER Tom Hennen (via rein) <287259336+agentcreds-validation-beef[bot]@users.noreply.github.com>
SBX| @PHASE3_START  push agent/351/<nonce> (expect: lands)
SBX| $ git push --progress origin HEAD:refs/heads/agent/351/author-20260719-022002-a561df
SBX| Enumerating objects: 4, done.
SBX| Counting objects: 100% (4/4), done.
SBX| Delta compression using up to 8 threads
SBX| Compressing objects: 100% (2/2), done.
SBX| Writing objects: 100% (3/3), 374 bytes | 374.00 KiB/s, done.
SBX| Total 3 (delta 1), reused 0 (delta 0), pack-reused 0
SBX| remote:
SBX| remote: Create a pull request for 'agent/351/author-20260719-022002-a561df' on GitHub by visiting:
SBX| remote:      https://github.com/TomHennen/agentcreds-validation-a/pull/new/agent/351/author-20260719-022002-a561df
SBX| remote:
SBX| To https://github.com/TomHennen/agentcreds-validation-a
SBX|  * [new branch]      HEAD -> agent/351/author-20260719-022002-a561df
SBX| @PHASE3_RC=0
SBX| @SCRIPT_DONE
rein: revoked 1 of 1 write token(s) on exit
