$ rein init --yes --no-alias --no-symlink  (real mint check, App from state.json, NO env vars)
rein init: setting up local scaffolding
  state dir:  /tmp/rein-itest-home-nc_psgk_/.local/state/rein
  config dir: /tmp/rein-itest-home-nc_psgk_/.config/rein
  machine:    demo-box (label for this machine's App name)
  setup:      state.json: audit_done (steady state from manifest flow)
  app:        client_id=Iv23liHH7Gu2xMxCZ7rz installation_id=138434780
  shim:       3 binaries under /tmp/rein-itest-home-nc_psgk_/.local/state/rein/shim
  symlink:    skipped (--no-symlink)
  mint check: minting a read-only installation token to verify App credentials
              ok (token expires 2026-07-19T16:04:05Z)
  session:    not scaffolded (pass --repo owner/name to scaffold)
  alias:      not installed (opt-in; enable with `rein init --alias`)
  install on repo:
    install the App on the repos you want it to broker tokens for.
    visit this URL in your browser:
      https://github.com/apps/rein-primary-735a3bbe20/installations/new
    (no local browser detected; open the link above on any machine — no ssh -L needed)
  nono:       installing pinned runtime 0.68.0 (aarch64-unknown-linux-gnu)
              ok (installed /tmp/rein-itest-home-nc_psgk_/.config/rein/nono/bin/nono, digest verified)

rein init: done.
Next:
  - /tmp/rein-itest-home-nc_psgk_/.config/rein/dev-session.yaml is repo-scoped; reads work now. Writes unlock per run when the agent runs `rein declare <n>` and you confirm (#35)
  - run `rein run -- claude` (or another agent) to launch with rein in effect
  - (optional) enable the `claude` alias with `rein init --alias`
  - run `rein doctor` to verify everything is wired up

$ rein run --direct -- git clone --quiet https://github.com/TomHennen/agentcreds-validation-a clone
===============================================================
rein: WARNING — DIRECT (UNSANDBOXED) MODE
  The agent runs OUTSIDE the nono sandbox. It shares your user
  account: it CAN read your ambient credentials (gh login, SSH
  keys, ~/.netrc, keyrings) and the rein-brokered token is only
  hidden by process boundaries, not a sandbox. Use this ONLY on a
  THROWAWAY repo. For real work, drop --direct to run sandboxed.
===============================================================
rein: launching wrapped process with:
  session: sess_init_then_run (role=implement, repos=[TomHennen/agentcreds-validation-a]) [source=file:/tmp/rein-itest-home-nc_psgk_/session.yaml]
  PATH-front shim dir: /tmp/rein-itest-home-nc_psgk_/.local/state/rein/shim
  per-process git config: /tmp/rein-run-613380043/gitconfig
  (your real ~/.gitconfig is layered in via include.path)

  Writes are LOCKED until the agent declares its issue:  rein declare <n>
  then push to agent/<n>/<nonce>. The declaration will prompt on THIS terminal
  (or a tmux popup); approving covers all writes for this run.

rein: running: git clone --quiet https://github.com/TomHennen/agentcreds-validation-a clone
---

$ rein run -- bash -c <sandboxed git clone TomHennen/agentcreds-validation-a> <workdir>
rein: worktrees: could not detect a GitHub repo for the working tree /tmp/rein-journey-sbxclone-3hcr4gw8 (git -C /tmp/rein-journey-sbxclone-3hcr4gw8 rev-parse --show-toplevel: exit status 128); the agent is not told which repo it is standing in
rein: verifying nono containment (creds hidden; loopback + direct egress denied)…
rein: containment WARNING: udp_egress: UDP egress OPEN (accepted residual — a prompt-injected agent has a UDP exfil channel) [8.8.8.8:53]
rein: containment gate passed.
rein: launching SANDBOXED (nono) run:
  session: sess_init_then_run (role=implement, repos=[TomHennen/agentcreds-validation-a]) [source=file:/tmp/rein-itest-home-nc_psgk_/session.yaml]
  GitHub egress tunnels through rein's loopback proxy: 127.0.0.1:35673
  the agent sees NO real token; git/gh are injected at the proxy (downstream of the sandbox).
  credentials are hidden (deny_credentials); the approval channel is isolated (af_unix_mediation).
  writable in sandbox (--allow): [/tmp/rein-journey-sbxclone-3hcr4gw8 /tmp/rein-itest-home-nc_psgk_/.config/rein-sandbox-home/.claude /tmp/rein-gh-1824857033]
  extra egress ALLOWED (direct, NOT injected, no rein token): [api.anthropic.com]
  writes are LOCKED until the agent declares its issue:  rein declare <n>
  the declaration will prompt on THIS terminal.
rein: running: bash -c set +e
emit() { printf '%s%s\n' 'SBX| ' "$*"; }
run() {
  printf '%s$ %s\n' 'SBX| ' "$*"
  "$@" 2>&1 | tr '\r' '\n' | while IFS= read -r l; do printf '%s%s\n' 'SBX| ' "$l"; done
  return ${PIPESTATUS[0]}
}
cd "$0"
run git clone --quiet https://github.com/TomHennen/agentcreds-validation-a clone
[ -d clone/.git ] && emit "@SBX_CLONE_RC=0" || emit "@SBX_CLONE_RC=1"
 /tmp/rein-journey-sbxclone-3hcr4gw8
---
  agent contract PRINTED to the agent's output (this agent has no system-prompt channel, so it may or
    may not reach the model's context; the REIN_IN_SANDBOX_* env vars carry the same facts machine-readably).
rein sandbox: you are running inside a sandbox managed by rein. These rules are ENFORCED, not advice.

FILES
- Your work persists ONLY in the working tree: /tmp/rein-journey-sbxclone-3hcr4gw8
  Write anything that must outlive this run THERE. Nothing else survives.
- $HOME is EPHEMERAL. Writes under it SUCCEED and read back normally, then are
  DISCARDED when the run ends. You will get no error. Do not keep work there.
- Most of $HOME reads as empty/missing; a few paths are re-exposed READ-ONLY, where
  writes fail with "Read-only file system". Caches work but start cold each run.

CREDENTIALS
- There are NO credentials in this environment. Tokens, keys, and credential stores
  are hidden on purpose. Do not go looking for them, and do not ask the user for one.
- git and gh still work anyway: a proxy OUTSIDE the sandbox injects a short-lived
  GitHub token you never see. Just use them normally.

GITHUB WRITES
- Writes to GitHub (push, PRs, comments, any API mutation) are LOCKED until you
  declare the issue this work is for:
      rein declare <n>
  That asks the human to approve, on their terminal. Reads work without it.
- Once confirmed, push ONLY to a branch named:
      agent/<n>/<nonce>
  where <n> is the declared issue number and <nonce> is a short name you choose
  (letters/digits, then letters/digits/./_/-). Any other ref is rejected.
- One issue per push.

NETWORK
- Egress is restricted to GitHub plus: api.anthropic.com
- Every other host is unreachable. A failed fetch is the sandbox, not a flaky network.

  nono v0.68.0
2026-07-19T15:04:08.691497Z  WARN Skipping Linux system temp grant '/tmp' from group 'system_write_linux' because HOME '/tmp/rein-itest-home-nc_psgk_' is nested inside it. Landlock cannot enforce deny rules beneath an allowed parent.
2026-07-19T15:04:08.691560Z  WARN Skipping Linux system temp grant '/tmp' from group 'system_write_linux' because HOME '/tmp/rein-itest-home-nc_psgk_' is nested inside it. Landlock cannot enforce deny rules beneath an allowed parent.
  Capabilities:
  ────────────────────────────────────────────────────
    r   /tmp/rein-nono-3402660995/ca-bundle.pem (file)
   r+w  /tmp/rein-journey-sbxclone-3hcr4gw8 (dir)
   r+w  /tmp/rein-itest-home-nc_psgk_/.config/rein-sandbox-home/.claude (dir)
   r+w  /tmp/rein-gh-1824857033 (dir)
    r   /tmp/rein-nono-3402660995/rein (file)
       + 46 system/group paths (-v to show)
   net  proxy
  ────────────────────────────────────────────────────

  mode supervised (proxy, supervisor)
  Applying sandbox...

SBX| $ git clone --quiet https://github.com/TomHennen/agentcreds-validation-a clone
SBX| @SBX_CLONE_RC=0
