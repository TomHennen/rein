$ rein run -- bash -c <sandbox gh-write script> /tmp/rein-ghwrite-mw_a3hs8
rein: sandbox preflight:
  [ok]   srt present: /home/admin/.npm-global/bin/srt
  [ok]   srt version: 0.0.63 (package.json; note `srt --version` misreports 1.0.0)
  [ok]   seccomp: apply-seccomp present (unix-socket block available)
  [ok]   bwrap userns: unprivileged user namespace works
  [ok]   system CA bundle: /etc/ssl/certs/ca-certificates.crt
rein: egress preset "dev" active (11 package-registry/advisory hosts; egress_preset: none in the session file turns it off)
rein: verifying sandbox config applies (deny-read + seccomp self-test)…
rein: sandbox self-test passed (credential stores hidden; unix sockets blocked; egress answered by rein).
rein: launching SANDBOXED (srt) run:
  session: sess_journey_gh_write (role=implement, repos=[TomHennen/agentcreds-validation-a]) [source=file:/tmp/rein-journey-sess-2avzyz__/session.yaml]
  proxy socket (out of sandbox): /run/user/1000/rein/run-ksCfEVrIGBNNo46WwilZzA/proxy.sock
  working tree (writable in sandbox): /tmp/rein-ghwrite-mw_a3hs8  [TomHennen/agentcreds-validation-a]
  a repo approved MID-RUN cannot be bound (bwrap binds are fixed at launch);
    the agent clones it into REIN_EPHEMERAL_CLONE_DIR=/tmp/rein-agent-tmp-3554336999
    (writable, DISCARDED at run end — the durable artifact is the push, not the tree;
     never inside the working tree, where a nested clone can be committed into it).
    map it under `worktrees:` to use your own checkout on the NEXT run.
  the agent sees NO real token; git/gh are injected at the proxy.
  credential stores, ~/.ssh, on-disk keyrings, and keyring/agent sockets are hidden.
  $HOME is HIDDEN in-sandbox. Three different behaviors — know which one you get:
    READS of a hidden path FAIL LOUDLY: ENOENT, or an empty listing for a dir.
    WRITES under hidden $HOME SILENTLY SUCCEED into a scratch tmpfs, then are
      DISCARDED at run end — caches (~/.cache, ~/.npm) work, but start cold every run.
    WRITES to the allowed-back paths below ERROR (read-only): 'Read-only file system'.
  NOTHING under $HOME PERSISTS. Only the working tree does: /tmp/rein-ghwrite-mw_a3hs8
    => work the agent must keep has to be written INTO the working tree.
    allowed back read-only: /home/admin/.npm-global/bin, /home/admin/.npm-global/lib/node_modules, /home/admin/.local/bin, /home/admin/.cargo, /home/admin/.rustup, /home/admin/go, /home/admin/.pyenv, /home/admin/.cache/ms-playwright
    if a tool breaks on a hidden $HOME path, allow it back narrowly:
      REIN_SANDBOX_ALLOW_READ=/abs/path[:/more] rein run --sandbox -- …
    or (NOT recommended — exposes your whole home dir): REIN_SANDBOX_SHOW_HOME=1
  extra egress ALLOWED (direct TLS, NOT injected, no rein token): *.crates.io, *.golang.org, *.npmjs.org, *.pypi.org, *.pythonhosted.org, *.rubygems.org, api.anthropic.com, crates.io, osv.dev, platform.claude.com, pypi.org, rubygems.org, vuln.go.dev
  writes are LOCKED until the agent declares its issue:  rein declare <n>
  then push to agent/<n>/<nonce>. The declaration will prompt on THIS terminal.
  to run bash WITHOUT rein for one command: `\bash` (bash/zsh) or `command bash` (fish)
  agent contract PRINTED to the agent's output (this agent has no system-prompt channel, so it may or
    may not reach the model's context; the REIN_IN_SANDBOX_* env vars carry the same facts machine-readably).
rein: running: bash -c
set +e
emit() { printf '%s%s\n' 'SBX| ' "$*"; }
run() {
  printf '%s$ %s\n' 'SBX| ' "$*"
  "$@" 2>&1 | tr '\r' '\n' | while IFS= read -r l; do printf '%s%s\n' 'SBX| ' "$l"; done
  return ${PIPESTATUS[0]}
}
cd "$0"

emit "@PHASE1_START  gh issue-comment write BEFORE declare (expect: rein declare-gate 403)"
run gh api -X POST /repos/TomHennen/agentcreds-validation-a/issues/531/comments -f body='rein gh-write journey: this comment was posted by a SANDBOXED `gh` after declare+approve. If you can read it, in-sandbox issue/PR writes work. Safe to delete.'
emit "@BEFORE_RC=$?"

emit "@PHASE2_START  rein declare 531 (blocks for the human on the host tty)"
run rein declare 531
emit "@DECLARE_RC=$?"

emit "@PHASE3_START  the SAME gh write AFTER declare (expect: HTTP 201, comment lands)"
run gh api -X POST /repos/TomHennen/agentcreds-validation-a/issues/531/comments -f body='rein gh-write journey: this comment was posted by a SANDBOXED `gh` after declare+approve. If you can read it, in-sandbox issue/PR writes work. Safe to delete.' --jq .body
emit "@AFTER_RC=$?"

emit "@PHASE4_START  commit + push agent/531/ghwrite-20260906-190143-9f43f6 (expect: push lands post-declare)"
run git checkout -b agent/531/ghwrite-20260906-190143-9f43f6
echo "gh-write journey PR probe for issue #531" >> gh-write-pr-probe.txt
run git add -A
run git commit -q -m "gh-write journey: PR probe for issue #531"
run git push origin HEAD:refs/heads/agent/531/ghwrite-20260906-190143-9f43f6
emit "@PUSH_RC=$?"

emit "@PHASE5_START  gh pr create (needs pull_requests:write AND a gated GraphQL read)"
base_ref=$(git symbolic-ref refs/remotes/origin/HEAD)
base="${base_ref##*/}"
run gh pr create --repo TomHennen/agentcreds-validation-a --base "$base" --head agent/531/ghwrite-20260906-190143-9f43f6 --title 'rein gh-write journey: PR leg (safe to close)' --body 'Opened by a SANDBOXED `gh pr create` after declare+approve, to prove pull_requests:write AND the GraphQL read gh pr create performs first both work in-sandbox (issue #101). Safe to close.'
emit "@PRCREATE_RC=$?"
emit "@SCRIPT_DONE"
 /tmp/rein-ghwrite-mw_a3hs8
---
rein sandbox: you are running inside a sandbox managed by rein. These rules are ENFORCED, not advice.

FILES
- Your work persists ONLY in the working tree: /tmp/rein-ghwrite-mw_a3hs8
  Write anything that must outlive this run THERE. Nothing else survives.
- $HOME is EPHEMERAL. Writes under it SUCCEED and read back normally, then are
  DISCARDED when the run ends. You will get no error. Do not keep work there.
- Most of $HOME reads as empty/missing; a few paths are re-exposed READ-ONLY, where
  writes fail with "Read-only file system". Caches work but start cold each run.
- A headless Chromium is available: Playwright browsers are host-installed under
  /home/admin/.cache/ms-playwright (read-only; found automatically by playwright/playwright-core).
  Use it to render, screenshot, and test web UIs. Do NOT run `npx playwright install`
  (that dir is read-only). If Playwright reports a missing chromium-<rev>, the host's
  version differs from your pinned playwright: ask the human to run `npx playwright
  install chromium` on the host, or pin the playwright version the host has.

CREDENTIALS
- There are NO credentials in this environment. Tokens, keys, and credential stores
  are hidden on purpose. Do not go looking for them, and do not ask the user for one.
- git and gh still work anyway: a proxy OUTSIDE the sandbox injects a short-lived
  GitHub token you never see. Just use them normally.

GITHUB WRITES
- Writes to GitHub (push, PRs, comments, any API mutation) are LOCKED until you
  declare the issue this work is for:
      rein declare <n>
  That asks the human to approve, on their terminal. Reads work without it.
- Once confirmed, push ONLY to a branch named:
      agent/<n>/<nonce>
  where <n> is the declared issue number and <nonce> is a short name you choose
  (letters/digits, then letters/digits/./_/-). Any other ref is rejected.
- One issue per push.

NETWORK
- Egress is restricted to GitHub plus: *.crates.io, *.golang.org, *.npmjs.org, *.pypi.org, *.pythonhosted.org, *.rubygems.org, api.anthropic.com, crates.io, osv.dev, platform.claude.com, pypi.org, rubygems.org, vuln.go.dev
- Package registries are OPEN by default (egress preset "dev"): Go modules, npm, PyPI, crates.io, RubyGems (plus the osv.dev / vuln.go.dev advisory feeds). Dependency installs work.
- Every other host is unreachable. A failed fetch is the sandbox, not a flaky network.
- You CANNOT open a blocked host yourself. To reach one, ask the human to run this
  single command, then restart rein (it applies to the NEXT run, not this one):
      rein session allow-domain <host>

SBX| @PHASE1_START  gh issue-comment write BEFORE declare (expect: rein declare-gate 403)
SBX| $ gh api -X POST /repos/TomHennen/agentcreds-validation-a/issues/531/comments -f body=rein gh-write journey: this comment was posted by a SANDBOXED `gh` after declare+approve. If you can read it, in-sandbox issue/PR writes work. Safe to delete.
SBX| {"message":"rein: no issue declared for this run. Run: rein declare <n> (the issue this work is for), approve on your terminal, then retry."}gh: rein: no issue declared for this run. Run: rein declare <n> (the issue this work is for), approve on your terminal, then retry. (HTTP 403)
SBX| @BEFORE_RC=1
SBX| @PHASE2_START  rein declare 531 (blocks for the human on the host tty)
SBX| $ rein declare 531

=== rein: agent declares work on an issue ===
   issue:    #531 "rein journey: gh-write walkthrough (safe to close)"  [open]
             in TomHennen/agentcreds-validation-a
   session:  sess_journey_gh_write (role=implement, repos=[TomHennen/agentcreds-validation-a])
   approving covers ALL writes for this run (git push, gh, API).

To approve, type the issue number (531) and press enter.
To deny, press Ctrl-C or type anything else.
> 531
  [approved]
SBX| issue #531 in TomHennen/agentcreds-validation-a confirmed — writes are unlocked for this run (push to agent/531/<nonce>)
SBX| @DECLARE_RC=0
SBX| @PHASE3_START  the SAME gh write AFTER declare (expect: HTTP 201, comment lands)
SBX| $ gh api -X POST /repos/TomHennen/agentcreds-validation-a/issues/531/comments -f body=rein gh-write journey: this comment was posted by a SANDBOXED `gh` after declare+approve. If you can read it, in-sandbox issue/PR writes work. Safe to delete. --jq .body
SBX| rein gh-write journey: this comment was posted by a SANDBOXED `gh` after declare+approve. If you can read it, in-sandbox issue/PR writes work. Safe to delete.
SBX| @AFTER_RC=0
SBX| @PHASE4_START  commit + push agent/531/ghwrite-20260906-190143-9f43f6 (expect: push lands post-declare)
SBX| $ git checkout -b agent/531/ghwrite-20260906-190143-9f43f6
SBX| Switched to a new branch 'agent/531/ghwrite-20260906-190143-9f43f6'
SBX| $ git add -A
SBX| $ git commit -q -m gh-write journey: PR probe for issue #531
SBX| $ git push origin HEAD:refs/heads/agent/531/ghwrite-20260906-190143-9f43f6
SBX| remote:
SBX| remote: Create a pull request for 'agent/531/ghwrite-20260906-190143-9f43f6' on GitHub by visiting:
SBX| remote:      https://github.com/TomHennen/agentcreds-validation-a/pull/new/agent/531/ghwrite-20260906-190143-9f43f6
SBX| remote:
SBX| To https://github.com/TomHennen/agentcreds-validation-a.git
SBX|  * [new branch]      HEAD -> agent/531/ghwrite-20260906-190143-9f43f6
SBX| @PUSH_RC=0
SBX| @PHASE5_START  gh pr create (needs pull_requests:write AND a gated GraphQL read)
SBX| $ gh pr create --repo TomHennen/agentcreds-validation-a --base main --head agent/531/ghwrite-20260906-190143-9f43f6 --title rein gh-write journey: PR leg (safe to close) --body Opened by a SANDBOXED `gh pr create` after declare+approve, to prove pull_requests:write AND the GraphQL read gh pr create performs first both work in-sandbox (issue #101). Safe to close.
SBX| https://github.com/TomHennen/agentcreds-validation-a/pull/532
SBX| @PRCREATE_RC=0
SBX| @SCRIPT_DONE
rein: revoked 1 of 1 write token(s) on exit
